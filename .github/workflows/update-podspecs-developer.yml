name: Update Developer Podspecs

on:
  workflow_call:
    inputs:
        name:
          required: true
          type: string
        version:
          required: true
          type: string

jobs:
  update-podspec:
    name: Upload Developer Podspecs
    runs-on: macos-latest
    steps:
      - name: Checkout Module Repository
        uses: actions/checkout@v3

      - name: Extracting HOMEPAGE from Podspec
        id: extract_homepage
        run: |
          PODSPEC_FILE=$(find . -name "*.podspec")
          HOMEPAGE=$(grep 's.homepage' $PODSPEC_FILE | sed -n -e 's/^.*s.homepage *= *//p'| tr -d '"' | tr -d "'")
          echo "HOMEPAGE=${HOMEPAGE}" >> $GITHUB_ENV

      - name: Updating Podspec for Developer Release
        run: |
          sed -i '' "s|s.version *= *['\"][^'\"]*['\"]|s.version          = '${{ inputs.version }}'|" ${{ inputs.name }}.podspec
          sed -i '' "s|s.source *= *{.*}|s.source           = { :git => \"${{ env.HOMEPAGE }}\", :tag => s.version.to_s }|" ${{ inputs.name }}.podspec

      - name: Checkout Specs Repository
        uses: actions/checkout@v3
        with:
          repository: "Carota-MarketPlanner/Specs"
          token: ${{ secrets.PAT_GITHUB }}
          path: specs_repo

      - name: Update Specs Repository with new Podspec
        run: |
          NAME=${{ inputs.name }}
          VERSION=${{ inputs.version }}
          mkdir -p specs_repo/${NAME}/${VERSION}
          cp ${NAME}.podspec specs_repo/${NAME}/${VERSION}/${NAME}.podspec

      - name: Commit and Push on Specs Repository
        run: |
          cd specs_repo
          git config --global user.email "github-actions@github.com"
          git config --global user.name "github-actions"
          git add ${{ inputs.name }}/${{ inputs.version }}/${{ inputs.name }}.podspec
          git commit -m "Atualizando o ${{ inputs.name }} para a versão ${{ inputs.version }}"
          git push